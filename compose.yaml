services:
  fontforge:
    build:
      context: .
      dockerfile: ./src/fontforge/Dockerfile
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./fonts:/fonts
      - ./build:/build
      - ./out/:/out
